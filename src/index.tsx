import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

export const websiteURL: URL = new URL("https://andresbrocco.com");

ReactDOM.render(<App />, document.getElementById('root'));
		
